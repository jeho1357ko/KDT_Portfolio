spring:
  # app 이름
  application:
    name: demo
  # DB연결 정보
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521/xe
    username: c##project
    password: project1234
  # Elasticsearch 설정
  elasticsearch:
    uris: http://localhost:9201
    connection-timeout: 30s
    socket-timeout: 30s

# 웹서버 포트
server:
  port: 9080

# 로그레벨
# trace > debug > info > warn > error > fatal
logging:
  level:
    root: info
    com.kh.project.domain.svc.YoutubeApiService: debug

elasticsearch:
  host: ${ES_HOST:localhost}
  port: ${ES_PORT:9200}
  scheme: ${ES_SCHEME:http}

# YouTube API 설정
youtube:
  api:
    key: ${YOUTUBE_API_KEY:AIzaSyAA6v8dm6osdoaR4AnZhZqjy_iGw9R5LxU}  # 환경변수에서 가져오거나 직접 입력
    max-results: 30  # 검색 결과 개수 더 증가 (고르는법 + 세척법)
    region-code: KR  # 지역 코드